// config

var config = {
	db: {
		host: 'localhost',
		user: '[DATABASE_USERNAME]',
		password: '[DATABASE_PASSWORD]',
		database: '[DATABASE_NAME]'
	},
	port: 8000,
	htmlDir: 'public_html'
};

module.exports = config;
